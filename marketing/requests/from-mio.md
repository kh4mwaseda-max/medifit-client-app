# 橘さんへ — 依頼3点への回答
**From: Mio（UIデザイン担当）**
**Date: 2026-04-12**
**Re: ユーザー獲得に必要なUI 3点**

---

橘さん、確認しました。コードを全部読んで現状を把握したので回答します。

---

## 依頼1: ランディングページ

**現状:** `/` に既にLPが存在します。ただし橘さんの要件を満たせていない部分が多い。

| 要件 | 現状 | 対応 |
|------|------|------|
| キャッチコピー「スクショを送るだけで、なぜ？が分かる」 | ❌ 別のコピーが入っている | 変更する |
| Before/After セクション | ❌ なし | 追加する（モックで先行） |
| 「トレーナーとして使う」「個人で使う」2ボタン分岐 | ❌ なし（「無料で始める」1本のみ） | 追加する |
| コース紹介5種 | ❌ なし | 追加する |
| 無料バッジ（ローンチ期間は全員無料） | ❌ なし（「クレジットカード不要」の小テキストのみ） | 追加する |
| デザイン: 黒/ダークネイビーベース | ❌ 現状は白ベース | 橘さんの希望通り変更する |

**橘さんへの確認事項（1点）:**
- 2プラン分岐の「個人で使う」はどこに飛ばしますか？
  - 個人オンボーディング（`/trainer/register` とは別フロー）は佐藤さんに確認中のはずです
  - 未実装なら「個人で使う → 登録ページ（準備中）」として先にLPだけ作って後でURLを差し替える形でよいですか？

---

## 依頼2: 招待フロー

**現状:** コード上に招待リンク生成の仕組みは**一切存在しません。**
佐藤さんへの to-sato.md の Q4 の答えは「ない」です（コードで確認済み）。

**Mio判断:** 招待フローはUI+バックエンド両方の実装が必要なので佐藤さんに依頼します。
佐藤さんへの追加指示書（`UI_DESIGN_BRIEF.md`）に追記しました。

実装イメージ（佐藤への指示に書いた内容）:
```
1. トレーナーダッシュボードに「招待リンク発行」ボタン
2. API: POST /api/trainer/invite → token生成 → clients テーブルに invite_token カラムを追加
3. 招待URL: [本番ドメイン]/join/[token]
4. クライアントが踏むと trainer_id が紐付いた状態で登録フローへ
5. 登録完了 → LINE通知 or 画面上のバッジで通知
```

---

## 依頼3: グラフSNSシェア

**現状:** 未実装。

**Mio判断:** Phase 1以降の対応で問題ないです（橘さんの優先度整理と同意見）。
ただし技術的に一点確認: グラフ画像の生成方法の選択肢は2つあります。

| 方法 | 工数 | 品質 |
|------|------|------|
| `html2canvas` でDOM要素をキャプチャ | 小 | 中（フォントずれあり） |
| `@vercel/og` でサーバーサイド画像生成 | 中 | 高（安定） |

Phase 1開始前に佐藤さんと方針決めます。

---

## 橘さんへの確認（2点まとめ）

1. 「個人で使う」CTAは準備中ページでよいか、それとも个人オンボーディングの実装を待ちますか？
2. LPのビジュアルトーンで「黒/ダークネイビー」指定がありましたが、AYFロゴの色はblue-600です。ダークネイビー（#0f172a系）ベースにblue-400/白アクセントで問題ないですか？

回答次第でLP実装に入ります。

Mio
